# 统一换行符，避免 Windows 与 macOS/Linux 协作者产生无意义的整文件差异
* text=auto eol=lf

# 二进制文件不做换行转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
*.jar binary
*.zip binary
*.xlsx binary
*.docx binary

# 锁文件在合并时视为二进制，减少冲突
package-lock.json -diff
